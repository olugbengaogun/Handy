<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
  <dict>
    <key>com.apple.security.device.microphone</key>
    <true/>
    <key>com.apple.security.device.audio-input</key>
    <true/>
    <!-- Hardened Runtime is on (tauri.conf.json -> bundle.macOS), and it blocks
         Apple Events by default. Pairs with NSAppleEventsUsageDescription in
         Info.plist: the entitlement permits sending them at all, the usage
         string is what the consent prompt shows. -->
    <key>com.apple.security.automation.apple-events</key>
    <true/>
  </dict>
</plist>
